% !TEX program = pdflatex
% PRL manuscript built around the final 180-kHz, 7.10-degree design.
% DATA-INTEGRITY GATE: Fig. 4 open symbols and the sampled field are currently
% exact theory subsamples.  Set \experimentaltrue only after replacing them
% with measured data and completing the experimental metadata in suppl.tex.
\documentclass[aps,prl,reprint,superscriptaddress,nofootinbib,floatfix]{revtex4-2}

\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath,amssymb,bm}
\usepackage{graphicx}
\usepackage[hidelinks]{hyperref}

\hypersetup{
  pdftitle={Rayleigh Bound State in the Continuum from Hybridized Acoustic Cavities},
  pdfauthor={Author names to be inserted},
  pdfsubject={Rayleigh BIC in an underwater acoustic metagrating}
}

\newif\ifexperimental
\experimentalfalse

\newcommand{\ii}{\mathrm{i}}
\newcommand{\e}{\mathrm{e}}
\newcommand{\BIC}{\mathrm{BIC}}
\newcommand{\RA}{\mathrm{RA}}
\newcommand{\homg}{\mathrm{hom}}

\begin{document}

\title{Rayleigh Bound State in the Continuum from Hybridized Acoustic Cavities}
\author{Author Name}
\affiliation{Affiliation to be inserted}
\author{Coauthor Name}
\affiliation{Affiliation to be inserted}
\date{\today}

\begin{abstract}
A diffraction channel carries no normal flux at a Rayleigh anomaly, but its
finite grazing field still prevents localization.  We realize a genuine
Rayleigh bound state in the continuum in an underwater acoustic lattice.  Two
dissimilar, individually radiating cavity modes form a Bloch-hybridized band
that reaches the first Rayleigh line with vanishing linewidth.  At the same
wavevector, the pressure amplitudes of both the grazing diffraction order and
the coexisting propagating order vanish because the two cavity contributions
have equal magnitudes and opposite phases in each channel.  Full-band
eigenvalue continuation, channel-resolved radiation zeros, and localized
eigenfields distinguish this state from a zero-flux Rayleigh anomaly.  The
result establishes hybridized-cavity double cancellation as a mechanism for
Rayleigh BICs in sound.
\end{abstract}

\maketitle

A bound state in the continuum (BIC) is an eigenstate that remains localized
although outgoing waves exist at the same real frequency.  Symmetry mismatch,
Friedrich--Wintgen interference, Fabry--P\'erot trapping, and topological
radiation zeros provide established routes to suppressing leakage
\cite{Friedrich1985,Hsu2016,Hsu2013,Yang2014,Zhen2014}.  Their common content is
stronger than a large quality factor: every asymptotic component of the
\emph{homogeneous} field that fails to decay must vanish.

This distinction becomes singular at a diffraction-channel opening.  At a
Rayleigh anomaly, the normal wavenumber of one Floquet order is zero, and so
is its normal energy flux, but its pressure remains finite and independent of
distance from the grating [Fig.~\ref{fig:concept}(b)].  The exterior norm then
grows without bound.  A Rayleigh anomaly is therefore not a BIC.
Electromagnetic reflection gratings have related trapped states to collisions
of leaky poles with Rayleigh branch points \cite{Monticone2017}, and recent
electromagnetic work has identified Rayleigh BICs through the conjunction of a
collective lattice resonance, a channel opening, and a nonradiating response
of the constituent scatterer \cite{Karavaev2026}.  Scattering matrices at
channel openings likewise possess nonanalytic square-root structure
\cite{Wojcik2021}.  These results establish the threshold as a distinct
spectral setting, but, to our knowledge, do not provide the
hybridized-cavity mechanism developed here for scalar sound.  Here and below,
``Rayleigh'' denotes a diffraction-order opening rather than an elastic
Rayleigh surface wave.

Acoustic BICs have been realized in open resonators, compact scatterers,
coupled cavities, and phononic crystals
\cite{Huang2021,Huang2022,Deriy2022,Marti2024,Krasikova2024,Yang2025Lines}.
Underwater Fabry--P\'erot BICs have also been observed
\cite{Yang2024Underwater}.  These advances do not answer the threshold
question addressed here: can a \emph{scalar} acoustic eigenmode remain bound
exactly when a new diffraction order opens while another order already
propagates?

Here we establish such a state using two dissimilar acoustic cavities, neither
of which is nonradiating on its own.  A broad small-cavity mode and a narrow
transverse mode of a large cavity form a Bloch-hybridized band.  The band is
brought to the $n=-1$ Rayleigh line at an off-$\Gamma$ wavevector where its
radiation vector has a common zero in two independent rows: the finite-flux
$n=0$ order and the grazing $n=-1$ order.  The Rayleigh line therefore fixes
the spectral endpoint, whereas cavity hybridization supplies the amplitude
and phase freedom needed to eliminate both nondecaying fields.  The resulting
state is a Rayleigh BIC not because the threshold flux vanishes, but because
the threshold field itself does.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/fig1_potential_refined_v2.pdf}
  \caption{\label{fig:concept}
  From a conventional BIC to a Rayleigh BIC.  (a) A conventional BIC cancels
  all open radiation.  (b) Off $\Gamma$, the $n=-1$ order is grazing at the
  Rayleigh anomaly while the $n=0$ order remains propagating; their finite
  amplitudes do not define a localized state.  (c) A Rayleigh BIC is obtained
  only when the grazing and propagating amplitudes share the common zero
  $A_{-1}=A_0=0$.}
\end{figure*}

\paragraph{Localization at a channel opening.---}
Consider a rigid surface periodic along $x$, with water in $y>0$ and time
dependence $\e^{+\ii\omega t}$.  A homogeneous outgoing field is
\begin{equation}
 p^{\homg}(x,y)=\sum_{n\in\mathbb Z}A_n
 \e^{-\ii k_{x,n}x-\ii k_{y,n}y},\quad
 k_{x,n}=k_x+\frac{2\pi n}{a},
 \label{eq:floquet}
\end{equation}
where $k_{y,n}=\sqrt{k_0^2-k_{x,n}^2}$ is chosen outgoing or decaying.
Introducing $\Omega=af/c$ and $\kappa=k_xa/(2\pi)$, the $n$th Rayleigh line
is $|\kappa+n|=\Omega$.  For one exterior order,
\begin{equation}
 \int_0^H |p_n|^2\,\mathrm{d}y=
 \begin{cases}
 H|A_n|^2, & k_{y,n}=0,\\[2pt]
 \dfrac{|A_n|^2}{2\alpha_n}(1-\e^{-2\alpha_nH}),
 & k_{y,n}=-\ii\alpha_n.
 \end{cases}
 \label{eq:norm}
\end{equation}
Equation~\eqref{eq:norm} is the decisive threshold criterion: zero normal
flux is insufficient; square integrability requires $A_n=0$ for a grazing
order, just as it requires zero amplitude for every propagating order.

\paragraph{Acoustic realization.---}
The unit cell [Fig.~\ref{fig:eigen}(a)] contains two water-filled grooves in a
rigid reflector.  At $c=1500~\mathrm{m\,s^{-1}}$, the dimensional realization
has $a=7.4167~\mathrm{mm}$ and a Rayleigh BIC at
\begin{equation}
 \begin{aligned}
 f_{\BIC}&=180.000~\mathrm{kHz},\\
 (\kappa_{\BIC},\Omega_{\BIC})
 &=(0.1100001053,\,0.8899998947),
 \end{aligned}
 \label{eq:bicpoint}
\end{equation}
corresponding to $\theta_{\BIC}=7.09966^{\circ}$.  At positive $\kappa$,
$n=0$ is propagating, $n=-1$ is grazing, and $n=+1$ is evanescent.  The
reciprocal partner at negative $\kappa$ exchanges the two first orders.

We solve a pole-free modal-matching eigenproblem retaining the exterior
Floquet amplitudes $\bm A$ and all transverse groove coefficients $\bm C$,
\begin{equation}
 \mathsf F(\Omega,\kappa)
 \begin{bmatrix}\bm A\\ \bm C\end{bmatrix}=0.
 \label{eq:fulloperator}
\end{equation}
  The outgoing-pole spectrum in Fig.~\ref{fig:eigen}(b) shows the target branch
connecting a symmetry-protected BIC at $\Gamma$ to the off-$\Gamma$ Rayleigh
BIC at Eq.~\eqref{eq:bicpoint}.  The latter occurs when the branch reaches the
$n=-1$ Rayleigh line; its imaginary frequency vanishes and the quality factor
diverges [Fig.~\ref{fig:eigen}(c)].  With 345 exterior orders and 43 transverse
modes per groove, the full operator at the exact endpoint has
$\sigma_{\min}/\sigma_{\max}=5.85\times10^{-16}$ and raw residual
$1.28\times10^{-13}$.  More importantly, its outgoing eigenvector satisfies
\begin{equation}
 A_0=A_{-1}=0,
 \label{eq:doublezero}
\end{equation}
while all retained exterior components are evanescent.  Channel-resolved
fields at the $\Gamma$ BIC, the Rayleigh BIC, and beyond the opening show the
corresponding channel topology; at the Rayleigh crossing, neither nondecaying
order survives [Fig.~\ref{fig:eigen}(d,e)].  Thus the diverging $Q$, the real-axis eigenvalue,
and the localized eigenfield are three views of the same homogeneous state.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/fig2_rayleigh_bic_eigenproblem.pdf}
  \caption{\label{fig:eigen}
  Rayleigh BIC in an underwater acoustic metagrating.  (a) Periodic
  two-groove surface and unit-cell dimensions.  (b) Outgoing-pole spectrum;
  color gives $\log_{10}Q$, with vivid colors denoting high $Q$.  Background
  poles are joined only over locally continuous, channel-consistent segments;
  open circles mark terminations at square-root cuts.
  The purple point is a symmetry-protected $\Gamma$ BIC, whereas the red
  points mark the reciprocal Rayleigh-BIC pair.
  (c) Local branch: $\mathrm{Re}\,\Omega_p$ reaches the $n=-1$ Rayleigh line
  as $Q$ diverges.  (d) Homogeneous Floquet pressure amplitudes: $A_0$ and
  $A_{-1}$ vanish together.  (e) Order-resolved exterior eigenfields at the
  $\Gamma$ BIC, the Rayleigh BIC, and beyond the opening.}
\end{figure*}

\paragraph{Origin of the double zero.---}
To expose the mechanism without replacing the full eigenproblem, we resolve
its aperture field into two dressed coordinates,
$\bm u=(u_L,u_S)^T$, dominated by the large- and small-groove mode families.
The groove-resolved boundary sources in the two relevant orders can be written
\begin{equation}
 \begin{bmatrix}J_0\\J_{-1}\end{bmatrix}
 =\mathsf D_s(\kappa)\bm u,
 \qquad
 \mathsf D_s=\begin{bmatrix}
 d_{0L}&d_{0S}\\ d_{-1,L}&d_{-1,S}
 \end{bmatrix}.
 \label{eq:radiationmatrix}
\end{equation}
For the propagating order, velocity continuity gives
$A_0=-(k_0/k_{y,0})J_0$; hence $J_0=0$ closes that channel.  At the Rayleigh
threshold, $J_{-1}=0$ enforces zero normal velocity but does not by itself
imply $A_{-1}=0$.  The latter pressure condition is supplied independently by
the complete eigenvector in Eq.~\eqref{eq:doublezero}.  The source reduction
therefore explains the cancellation mechanism, while the full operator
establishes strict localization.

The Bloch phase changes the hybrid eigenvector [Fig.~\ref{fig:mechanism}(a,b)].
At $\kappa_{\BIC}$, the radiation matrix becomes rank deficient and the
eigenvector enters its common dark subspace [Fig.~\ref{fig:mechanism}(c)].
Equivalently, the two groove
contributions $j_{mL}=d_{mL}u_L$ and $j_{mS}=d_{mS}u_S$ obey
\begin{equation}
 \begin{aligned}
 |j_{mL}|&=|j_{mS}|,\\
 \arg\!\left(\frac{j_{mL}}{j_{mS}}\right)&=\pi,
 \qquad m=0,-1.
 \end{aligned}
 \label{eq:phasorcondition}
\end{equation}
Thus $J_0=J_{-1}=0$ at the same Bloch wavevector
[Fig.~\ref{fig:mechanism}(d)].  Neither isolated groove is nonradiating;
periodicity instead selects a hybrid cavity eigenvector whose channel sources
cancel in both rows, while the exact pressure amplitudes simultaneously obey
Eq.~\eqref{eq:doublezero}.  Calculations for the isolated grooves, the
nonperiodic dimer, and a bounded single-groove search are given in the
Supplemental Material.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/fig3_common_dark_state_nature.pdf}
  \caption{\label{fig:mechanism}
  Common-dark-state origin of the Rayleigh BIC.  (a) Two hybrid cavity
  coordinates radiate into the $n=0$ and $n=-1$ rows.  (b) Bloch evolution of
  the large- and small-cavity modal weights.  (c) Simultaneous collapse of the
  radiation-matrix singular-value ratio and the eigenstate--dark-state
  misalignment.  (d) At the BIC, the two groove contributions cancel as
  complex phasors in both radiation rows.  The strict pressure zeros are
  obtained from the full eigenproblem in Fig.~\ref{fig:eigen}.}
\end{figure*}

\paragraph{Scattering observables.---}
A strict BIC is dark under an ideal plane-wave drive; it is inferred from the
collapse of the neighboring quasi-BIC response rather than from a finite
field exactly at the BIC.  Figure~\ref{fig:experiment}(b) therefore plots the
channel-resolved resonant contrast in coordinates centered on the complex
pole.  Both scattering lines narrow to zero at $\theta_{\BIC}$; physical-frequency
cuts expose this collapse, while the fitted linewidth and integrated channel
spectral weights vanish toward the dark point
[Fig.~\ref{fig:experiment}(c,d)].  A complex pressure scan above the sample can
be Fourier projected onto the Floquet orders, providing a direct
channel-resolved measurement [Fig.~\ref{fig:experiment}(a,e)].

\ifexperimental
The measured symbols follow the predicted linewidth collapse and the
simultaneous suppression of both channel residues.  The measured complex
field and its spatial spectrum reproduce the two accessible Floquet
components away from the dark point.  Experimental dimensions, calibration,
uncertainties, and finite-array controls are reported in the Supplemental
Material.
\else
In the present theory draft, the open symbols and the coarsely sampled field
are exact subsamples of the calculations, chosen on the intended experimental
grid.  They define the data products that must be replaced by measurements;
they are not presented as experimental validation.
\fi

\begin{figure*}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/fig4_scattering_observability_nature.pdf}
  \caption{\label{fig:experiment}
  Observable scattering signatures.  (a) Water-tank complex-field mapping.
  (b) Simultaneous collapse of the background-subtracted $n=0$ and $n=-1$
  scattering linewidths; white curves denote the pole half-linewidth.
  (c) Physical-frequency cuts of the normalized specular resonant contrast.
  (d) Extracted pole linewidth and integrated spectral weights of the two
  propagating orders.  (e) Reference-subtracted complex-field sampling and
  Floquet reconstruction at a nearby driven state; an exact BIC cannot be
  excited by the external plane wave.
  \ifexperimental Open symbols and the sampled field are measurements;
  continuous maps and curves are theory.\else Open symbols and the sampled
  field are theory subsamples reserved for future measured data.\fi}
\end{figure*}

The result identifies a mechanism for a Rayleigh BIC in an underwater
acoustic lattice.  The vanishing channel velocity explains the zero normal
flux, but not localization.  Localization emerges only when a hybridized
cavity Bloch eigenvector is simultaneously orthogonal to the grazing and
propagating pressure-radiation rows.  The two-groove surface realizes this
common null through interference between a narrow transverse cavity mode and
a broad radiative cavity mode.  Consequently, the central state is neither a
generic Rayleigh anomaly nor a merely high-$Q$ resonance: it is a Rayleigh BIC
in a sound-wave continuum, created when a hybridized cavity band reaches the
diffraction threshold with both nondecaying radiation amplitudes canceled.

The ideal statement applies to an infinite, rigid, lossless grating.
Finite aperture, material compliance, viscous attenuation, and disorder turn
the BIC into a quasi-BIC and bound the experimentally accessible $Q$.  These
effects do not alter the amplitude-level criterion in
Eq.~\eqref{eq:doublezero}, but they must be included in a quantitative
comparison with a finite water-tank sample.

\begin{acknowledgments}
Acknowledgments and funding information to be inserted.
\end{acknowledgments}

\bibliography{ref}

\end{document}
